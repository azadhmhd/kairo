name: kairo_scheduler
description: "Kairo's time engine: it knows when something is due, and nothing else."
version: 0.0.1

publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1

  kairo_event_bus:
    path: ../kairo_event_bus

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
