# Kairo's shared rules. Change them in the repository's analysis_options.yaml
# rather than here, so the application is held to the same standard as the
# packages it depends on.
include: ../../analysis_options.yaml
